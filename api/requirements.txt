# Agentic AI Portfolio - API Requirements
# Compatible Python version: 3.11 (pinned via .python-version)
#
# This file is used by Render for deployment of the FastAPI server only.
# It is intentionally lean — CrewAI, MCP, and other pipeline-only
# dependencies are excluded to keep build times and memory usage low.
#
# FAISS vector databases are committed to the repo (*.faiss, *.pkl)
# and loaded at startup — no rebuild needed on deployment.
#
# Build command: pip install -r api/requirements.txt && python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2')"
# Start command: uvicorn api.main:app --host 0.0.0.0 --port $PORT

# ── ASGI server ─────────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.41.0

# ── LangChain stack ─────────────────────────────────────────
langchain==1.2.10
langchain-community==0.4.1
langchain-core==1.2.18
langchain-groq==1.1.2
langchain-huggingface==1.2.1
langchain-tavily==0.2.17
langchain-text-splitters==1.1.1

# ── LangGraph ───────────────────────────────────────────────
langgraph==1.0.10
langgraph-checkpoint==4.0.1
langgraph-prebuilt==1.0.8
langgraph-sdk==0.3.10

# ── HuggingFace stack ───────────────────────────────────────
# Model is pre-downloaded during build phase (see build command above)
huggingface-hub==1.6.0
transformers==5.3.0
tokenizers==0.22.2
sentence-transformers==5.2.3
safetensors==0.7.0

# ── Groq ────────────────────────────────────────────────────
groq==0.37.1

# ── Pydantic ────────────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1

# ── Vector search ───────────────────────────────────────────
faiss-cpu==1.13.2

# ── Web search ──────────────────────────────────────────────
tavily-python==0.7.23

# ── PDF / Web loading ───────────────────────────────────────
pypdf>=3.0.0
beautifulsoup4>=4.12.0

# ── Utilities ───────────────────────────────────────────────
python-dotenv==1.2.2